apiVersion: v1
kind: Service
metadata:
  name: coordinator-agent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "coordinator-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "coordinator-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
