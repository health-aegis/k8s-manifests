{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: coordinator-agent-netpol
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "coordinator-agent.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "coordinator-agent.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: api-gateway
      ports:
        - protocol: TCP
          port: 8000
  egress:
    - {}
{{- end }}
