apiVersion: v1
kind: Service
metadata:
  name: patient-history-agent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "patient-history-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "patient-history-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
