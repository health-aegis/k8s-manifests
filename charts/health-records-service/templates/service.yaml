apiVersion: v1
kind: Service
metadata:
  name: health-records-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "health-records-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "health-records-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
