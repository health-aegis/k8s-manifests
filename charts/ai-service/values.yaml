replicaCount: 1

image:
  acrLoginServer: aegisacraswin.azurecr.io
  repository: ai-service
  pullPolicy: Always
  tag: "latest"

service:
  type: ClusterIP
  port: 5003
  targetPort: 5003

resources:
  requests:
    cpu: 100m
    memory: 192Mi
  limits:
    cpu: 500m
    memory: 512Mi

hpa:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  cpuUtilization: 70
  memoryUtilization: 80

probes:
  readiness:
    path: /healthz
    initialDelaySeconds: 10
    periodSeconds: 8
    timeoutSeconds: 3
    failureThreshold: 3
  liveness:
    path: /healthz
    initialDelaySeconds: 20
    periodSeconds: 15
    timeoutSeconds: 3
    failureThreshold: 3

env:
  PORT: "5003"
  NODE_ENV: production

secrets:
  providerClassName: aegis-kv-secret-provider
  kvSecretName: aegis-kv-secrets

configMap:
  name: aegis-config

serviceAccount:
  name: aegis-workload-identity

networkPolicy:
  enabled: true
